export interface ITheme {
    colors: {
        gray: string,
        white: string,
        red: string,
        dark: string,
        darkGray: string,
    }
    fonts: {
        montserrat: string,
    }
}
